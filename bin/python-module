#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
module=${1:?Usage: python-module MODULE [ARGUMENT ...]}
shift

exec "${root}/bin/python3" -m "${module}" "$@"
