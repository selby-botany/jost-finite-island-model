#!/usr/bin/env bash

root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
exec "${root}/bin/python3" -c \
    'import sys; from pydoc_markdown.main import cli; sys.argv[0] = "pydoc-markdown"; cli()' \
    "$@"
